.. This page is the repository's CHANGELOG.md, rendered. Release notes are
   written there and nowhere else: the file is what contributors edit, what
   `release.yml` extracts the GitHub release body from, and what this page
   shows. Keeping a second copy here is what let the two drift apart before —
   each had entries the other did not.

.. include:: ../../CHANGELOG.md
   :parser: myst_parser.sphinx_
